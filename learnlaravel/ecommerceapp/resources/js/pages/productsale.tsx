export default function ProductSale({ PSTitle, PSText }) {
        return (
                <div>
                        <div>
                                <h1>{PSTitle}</h1>
                                <p>{PSText}</p>
                        </div>
                        <div>

                        </div>
                </div>
                
        )
};